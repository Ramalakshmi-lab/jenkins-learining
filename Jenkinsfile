pipeline {
    agent {label 'agent-1' }
    tools{

        maven 'Maven 3.8.4'
    }
    stages {
        stage('First') {
            steps {
                script {
                    echo 'hi this is rambo'
                }
            }
        }
    }
}
