pipeline {
    agent {label: agent-1}

    stages {
        stage('First') {
            steps {
                script {
                    echo 'hi this is rambo'
                }
            }
        }
    }
}
